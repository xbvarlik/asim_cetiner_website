# Data Model: 015-header-services-cta-layout

## Scope

This feature changes **layout and presentation** only. It does not introduce or modify database entities, API payloads, or server-side validation.

## Entities

None.

## Notes

- Navigation labels and `ROUTES` remain defined in code as today (`NavItem[]`, `lib/routes.ts`).
- Service copy remains static in `services-list-cards.tsx` (`SERVICES` constant).
