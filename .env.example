DATABASE_URL="postgresql://user:password@localhost:5432/asim_cetiner_dev?schema=public"

# Public site origin (no trailing slash). Used for JSON-LD @id/url and /llms.txt absolute links.
# See specs/022-aeo-llms-jsonld/quickstart.md
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# Required in production: random secret for admin session JWT (HS256)
ADMIN_JWT_SECRET="change-me-to-a-long-random-string"
